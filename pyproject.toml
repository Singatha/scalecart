[tool.ruff]
target-version = "py312"
line-length = 100
src = ["packages/python-common/src"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "ASYNC"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["packages/python-common/tests"]

